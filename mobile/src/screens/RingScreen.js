import { createPlaceholderScreen } from './placeholderScreen';

// The critical screen: full ring lifecycle over the lock screen (spec §6.3, task 7).
export default createPlaceholderScreen('RingScreen');
