import { createPlaceholderScreen } from './placeholderScreen';

// Notifications, exact alarm, battery-optimisation exemption and OEM autostart
// (spec §6.4, task 8).
export default createPlaceholderScreen('PermissionsScreen');
